package transform

import (
	"testing"
)

func TestTransformation(t *testing.T) {
	// body := []byte(`text`)

	// tr := &Json{}

	// newBody, err := tr.Transform(body)
	// require.NoError(t, err)
	// require.NotEqual(t, newBody, body)

	// fmt.Println(string(newBody))
}
